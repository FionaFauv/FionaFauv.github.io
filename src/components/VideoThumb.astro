---
interface Props {
  src?: string;
  alt: string;
  duration?: string;
  /** Pastille en haut à gauche (« Rediff », « Temps fort »…). */
  badge?: string;
  loading?: 'lazy' | 'eager';
}

// Miniature 16/9 d'une vidéo ou d'une rediffusion, avec la durée en bas à droite.
const { src, alt, duration, badge, loading = 'lazy' } = Astro.props;
---

<div class="placeholder-art relative aspect-video overflow-hidden rounded-[10px]">
  {
    src && (
      <img
        src={src}
        alt={alt}
        class="absolute inset-0 size-full object-cover"
        loading={loading}
        decoding="async"
        width="640"
        height="360"
        onerror="this.remove()"
      />
    )
  }
  {
    badge && (
      <span class="absolute top-1.5 left-1.5 rounded-[5px] bg-live px-1.5 py-1 text-[10px] leading-none font-semibold tracking-wide text-white uppercase">
        {badge}
      </span>
    )
  }
  {
    duration && (
      <span class="absolute right-1.5 bottom-1.5 rounded-[5px] bg-black/80 px-1.5 py-1 font-mono text-[11px] leading-none text-white">
        {duration}
      </span>
    )
  }
</div>
